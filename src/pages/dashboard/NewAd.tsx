// Other imports

// ...

// Actual implementation

invoke("generate-google-ads", {
    businessName,
    description,
    budget,
    goal,
    radius,
    adId,
    generateVariants,
});

// Other code
